<h1>Patients</h2>
<ul>
<% @patients.each do |patient| %>
  <li>
    <a href="<%= patient_path(patient) %>">
    Name: <%= patient.name %> Number of Appointments: <%= patient.appointments.size %>
  </li>
<% end %>

</ul>
